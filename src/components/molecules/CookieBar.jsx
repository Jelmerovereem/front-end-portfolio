// @ts-check

import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  position: fixed;
  bottom: 20px;
  background-color: white;
`;

const CookieBar = () => {
  return <Wrapper>Accepteer cookies</Wrapper>;
};

export default CookieBar;
